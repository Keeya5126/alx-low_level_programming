#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success/correct)
 */
int main(void)
{
	char kep;

	for (kep = 'a'; kep <= 'z'; kep++)
	{
		putchar(kep);
	}
	putchar('\n');
	return (0);
}
