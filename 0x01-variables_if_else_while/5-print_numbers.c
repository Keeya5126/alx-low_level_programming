#include <stdio.h>

/**
 * main - Entry point
 * author: keeya
 * Return: Always 0 (Success/correct)
 */
int main(void)
{
	int p;

	for (p = 0; p < 10; p++)
	{
		printf("%d", p);
	}
	printf("\n");
	return (0);
}
